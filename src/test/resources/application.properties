#EnableSwagger2
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

#Liquibase
spring.liquibase.enabled=false

#jwt
jwt.tokenHeader = Authorization
jwt.tokenPrefix = Bearer 
jwt.jwtSecretKey = ${secretKey}
jwt.expTime = 259200000